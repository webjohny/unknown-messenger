/**
 * Rooms are drawn by the skin mounted in the root layout, which reads the id
 * from the URL. This page only makes `/room/:roomId` a real route; see
 * `app/SkinHost.tsx`.
 */
export default function RoomPage() {
  return null;
}
