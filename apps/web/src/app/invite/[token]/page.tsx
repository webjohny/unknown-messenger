/**
 * Invite links are drawn by the skin mounted in the root layout, which reads
 * the token from the URL. This page only makes `/invite/:token` a real route;
 * see `app/SkinHost.tsx`.
 */
export default function InvitePage() {
  return null;
}
